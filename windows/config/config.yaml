# 自动化控制器全局配置

# ===== 模拟器配置 =====
emulator:
  adb_port: "127.0.0.1:16448"  # MuMu 12 默认端口

# ===== 浏览器配置 =====
browser:
  headless: true          # 无头模式
  timeout: 60000          # 超时时间（毫秒）
  download_path: "./data/downloads"  # 下载路径
